# Local environment and secrets
.env

# Docker / standalone server runtime data
data/
*.db
sv.db

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Steam / GMod install (when SERVER_DIR points inside the repo)
gmod-server/
